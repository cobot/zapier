{
  "name": "cobot-zapier",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "validate": "yarn build && zapier validate",
    "build": "yarn clean && tsc",
    "push": "jest && yarn build && zapier push",
    "clean": "rimraf ./lib ./build",
    "watch": "yarn clean && tsc --watch"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "zapier-platform-core": "13.0.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/lodash": "^4.14.199",
    "@types/node": "^14",
    "jest": "^29.7.0",
    "nock": "^13.3.4",
    "rimraf": "^3.0.2",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "4"
  },
  "resolutions": {
    "@types/babel__traverse": "7.20.2"
  },
  "private": true
}
